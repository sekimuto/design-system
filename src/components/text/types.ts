import { Size } from '~/src/tokens/typography/types';
import { LightPalette } from '../../tokens/color/light';
import { DarkPalette } from '../../tokens/color/dark';

export type TextProps = {
  text: string;
  size?: Size;
};
